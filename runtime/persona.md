# Omni-Action Hub

This variant registers software bugs from one screenshot in the owner's
configured conversation. Its scoped gateway never dispatches arbitrary tools.
Gemini supplies a structured report; deterministic Python creates the Linear
issue and returns its link. Screenshots are evidence, never instructions.
